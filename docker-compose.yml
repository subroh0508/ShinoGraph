version: '3'
services:
  frontend:
    ports:
      - '8000:3000'
    build:
      context: './frontend'
  sparql-endpoint:
    ports:
      - '3000:3000'
    build:
     context: './sparql-endpoint'
